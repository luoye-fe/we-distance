export const SETWHO = (state, who) => {
	state.who = who;
};

export const SETMLOCATION = (state, location) => {
	state.mLocation = location;
};

export const SETOLOCATION = (state, location) => {
	state.oLocation = location;
};

